# mcp — Node MCP server (stdio).
.PHONY: dev start build install docker

install:
	npm install

dev:
	npm run dev

start:
	npm run start

build:
	npm run build

docker:
	docker build -t meergate-mcp .
